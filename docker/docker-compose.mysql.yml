version: "3.8"
services:
  mysql-test:
    image: mysql:8.0
    container_name: sql-constructor-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: test_db
    ports:
      - "3306:3306"
    tmpfs:
      - /var/lib/mysql
